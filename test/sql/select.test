# name: test/sql/select.test
# description: Basic
# group: [sql]

require adbc

query I
SELECT * FROM read_adbc('profile://./test/penguins.toml', 'SELECT species FROM penguins ORDER BY species LIMIT 1')
----
Adelie
